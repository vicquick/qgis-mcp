[project]
name = "qgis-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # 1.10+ is required for structured_output / annotations / meta,
    # which the qtool wrapper depends on.
    "mcp[cli]>=1.27.0",
    # window capture for the screenshot tool
    "pillow>=10.4",
]
